# 变更日志 - 2026-01-26 23:51:00

## 任务描述
将下载并发数从系统设置移至任务级别配置

## 执行结果

### 修改内容

1. **数据库** (`src/main/database/index.ts`)
   - `download_tasks` 表新增 `concurrency` 字段（默认值: 3）
   - 移除 `download_concurrency` 系统设置
   - 添加迁移脚本为已存在的表添加列

2. **类型定义** (`src/preload/index.d.ts`)
   - `DbTask` 接口添加 `concurrency: number`
   - `TaskAPI.create` 和 `update` 支持 `concurrency` 参数

3. **系统设置页** (`SystemPage.tsx`)
   - 移除并发数配置 UI
   - 简化下载设置卡片布局
   - 更新说明：0 表示不限制

4. **下载任务页** (`DownloadPage.tsx`)
   - 新增 `concurrency` 状态
   - 任务弹框添加并发数输入框
   - 创建/编辑任务时保存并发数

### 并发数说明
- 范围: 1-10
- 含义: 同时下载多少个用户的视频
- 配置位置: 每个任务独立配置
